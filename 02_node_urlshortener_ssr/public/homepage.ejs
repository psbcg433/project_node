<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cyberpunk URL Shortener</title>
    <link rel="stylesheet" href="homepage.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="background-overlay"></div>
    
    <header class="header">
        <h1>⚡ Cyberpunk URL Shortener ⚡</h1>
        <h2>Shorten your URLs with a futuristic touch</h2>
    </header>
    
    <main class="container">
        <form action="/api/url/" method="POST" class="url-form">
            <input type="url" name="originalURL" placeholder="Enter your URL..." required class="url-input">
            <button type="submit" class="submit-btn">Shorten 🚀</button>
        </form>
        
        <table class="url-table">
            <thead>
                <tr>
                    <th>🔢 Serial</th>
                    <th>🌍 Original URL</th>
                    <th>🔗 Shortened URL</th>
                    <th>📊 Visits</th>
                </tr>
            </thead>
            <tbody>
                <% urls.forEach((url, index) => { %>
                    <tr>
                        <td><%= index + 1 %></td>
                        <td class="long-url"><%= url.redirectURL%></td>
                        <td><a href="http://localhost:3000/<%= url.shortID%>" class="short-url">http://localhost:3000/<%= url.shortID%></a></td>
                        <td><%= url.clicks %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </main>
</body>
</html>
